services:
  geocache:
    build: .
    container_name: geocache-server
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DEFAULT_TTL=3600
      - MAX_CACHE_SIZE=1000
    restart: always